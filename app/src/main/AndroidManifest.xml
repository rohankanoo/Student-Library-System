<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rohankanoo.homepage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TestingActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".StudentInfoActivity" />
        <activity android:name=".StudentSignupActivity" />
        <activity android:name=".EmployeeInfoActivity" />
        <activity android:name=".EmployeeSignupActivity" />
        <activity android:name=".StudentProfileActivity" />
        <activity android:name=".StudentOptionsActivity"></activity>
    </application>

</manifest>